# Housekeeper - 智能笔记管理系统

一个现代化的全栈笔记管理系统，支持共享看板功能，提供优雅的Web界面。

## ✨ 特性

- 📱 **响应式设计** - 完美支持桌面端、平板和手机
- 🎨 **主题切换** - 深色/浅色主题随心切换
- 🚀 **高性能** - Vue 3 + Vite + Go 构建
- 🔐 **安全认证** - JWT身份验证
- 📋 **共享看板** - 跨设备共享剪贴板内容
- 🌍 **国际化** - 支持多语言
- 💾 **数据持久化** - MongoDB + Redis

## 🏗️ 技术栈

### 前端
- Vue 3 + TypeScript
- Vue Router 4
- Pinia（状态管理）
- Axios（HTTP客户端）
- Vite（构建工具）

### 后端
- Go 1.21+
- Gin（HTTP框架）
- MongoDB（数据存储）
- Redis（缓存）
- JWT（认证）

## 🚀 快速开始

### 前置要求

- Go 1.21+
- Node.js 18+
- MongoDB 6+
- Redis 7+

### 安装

```bash
# 克隆项目
git clone <repository-url>
cd housekeeper

# 安装依赖
cd web
npm install
cd ..
```

### 开发模式

#### 方式一：分别启动前后端（推荐开发时使用）

```bash
# 终端1：启动后端
make dev
# 或
go run . -c ./internal/conf/config.toml

# 终端2：启动前端
cd web
npm run dev
```

前端开发服务器：http://localhost:3000
后端API服务器：http://localhost:19090

#### 方式二：仅启动后端（使用已构建的前端）

```bash
# 先构建前端
make build-web

# 启动后端
make run
```

访问：http://localhost:19090

### 生产构建

```bash
# 完整构建（前端+后端）
make build

# 运行
./housekeeper -c ./internal/conf/config.toml
```

### Docker 部署

```bash
# 构建并启动所有服务（Housekeeper + Redis + MongoDB）
make docker-up

# 查看服务状态
make docker-status

# 查看日志
make docker-logs

# 停止服务
make docker-down
```

访问：http://localhost:19090

详细的构建和部署说明，请参考：
- [前端构建说明](web/BUILD.md)
- [部署指南](DEPLOYMENT.md)
- [Docker 部署指南](deploy/README.md)

## 📖 项目结构

```
housekeeper/
├── web/                    # 前端项目
│   ├── src/
│   │   ├── api/           # API接口
│   │   ├── components/    # 组件
│   │   ├── composables/   # 组合式函数
│   │   ├── layouts/       # 布局
│   │   ├── router/        # 路由
│   │   ├── stores/        # 状态管理
│   │   ├── types/         # 类型定义
│   │   ├── utils/         # 工具函数
│   │   └── views/         # 页面
│   ├── dist/              # 前端构建输出（自动生成）
│   ├── package.json
│   └── vite.config.ts
├── internal/              # 后端项目
│   ├── conf/             # 配置
│   ├── handler/          # HTTP处理器
│   ├── model/            # 数据模型
│   ├── repository/       # 数据访问层
│   ├── router/           # 路由
│   └── service/          # 业务逻辑
├── Makefile              # 构建脚本
├── bootstrap.go          # 程序入口
├── go.mod
└── README.md
```

## 🔧 配置

编辑 `internal/conf/config.toml`:

```toml
# 服务器配置
port = 19090

# MongoDB配置
[mongodb]
uri = "mongodb://localhost:27017"
database = "housekeeper"

# Redis配置
[redis]
addr = "localhost:6379"
db = 1

# JWT配置
[jwt]
secret = "your-secret-key"
expire = 7200  # 2小时
```

## 📝 API文档

### 认证接口

```
POST /v1/user/login       # 用户登录
```

### 看板接口

```
POST   /v1/notes/share_board/create    # 创建看板
POST   /v1/notes/share_board/list      # 查询看板列表
PUT    /v1/notes/share_board/update    # 更新看板
DELETE /v1/notes/share_board/delete    # 删除看板
```

所有看板接口需要JWT认证。

## 🎨 功能特性

### 1. 用户认证
- 登录/登出
- JWT Token管理
- 自动刷新Token

### 2. 共享看板
- 创建/编辑/删除看板
- 实时设备信息获取
- 内容一键复制
- 分页查询
- 筛选功能

### 3. 主题系统
- 深色主题
- 浅色主题
- 主题持久化

### 4. 响应式设计
- 桌面端：侧边栏导航
- 移动端：抽屉式导航
- 自适应布局
- 触摸优化

## 🐛 故障排查

### 前端构建失败

如果遇到 vue-tsc 错误：

```bash
cd web
rm -rf node_modules package-lock.json
npm install
npm run build
```

### 后端启动失败

检查配置文件和数据库连接：

```bash
# 检查MongoDB
mongosh

# 检查Redis
redis-cli ping
```

### 端口冲突

修改 `internal/conf/config.toml` 中的端口配置。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可

MIT License

## 📞 联系方式

如有问题，请提交 Issue。

---

**Housekeeper** - 让笔记管理更简单 ✨
